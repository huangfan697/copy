server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://124.222.183.199:8808/wrong_note?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 阿里云 OSS 配置
aliyun:
  oss:
    endpoint: ${OSS_ENDPOINT:oss-cn-hangzhou.aliyuncs.com}
    access-key-id: ${OSS_ACCESS_KEY_ID:}
    access-key-secret: ${OSS_ACCESS_KEY_SECRET:}
    bucket-name: ${OSS_BUCKET_NAME:wrong-note}
    url-prefix: ${OSS_URL_PREFIX:https://wrong-note.oss-cn-hangzhou.aliyuncs.com/}

# 通义千问 API 配置
dashscope:
  base-url: ${DASHSCOPE_BASE_URL:http://newapi.raycloud.cn}
  api-key: ${DASHSCOPE_API_KEY:sk-XzUxZaRbWTTo0oUePIZuiUwyspiE4fWSSQe7fPIiV7DnUAsM}
  model: ${DASHSCOPE_MODEL:qwen3.5-plus}

# 微信小程序配置
wechat:
  app-id: ${WECHAT_APP_ID:wx2ac2f831477f1535}
  app-secret: ${WECHAT_APP_SECRET:08b171cf9c298a1f632dd7fb2daa0b37}
